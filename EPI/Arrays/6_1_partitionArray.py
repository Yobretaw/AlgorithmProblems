import sys
import os
import re
import math
import random

"""
    Write a function that takes an array A of length n and an index i into A,
    and rearrange the elements such that all elements less than A[i] appear
    first, followed by elements equal to A[i], followed by elements greater
    than A[i]
"""
def rearrange(a, i):
    pass



l = [4, 3, 1, 4, 6, 3, 1, 2]
rearrange(l, 1)
print l
